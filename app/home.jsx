import { Text, View } from 'react-native'
import { ScrollView } from 'react-native-web'

const home = () => {
  return (
    <ScrollView>
      <View>
        <Text>home</Text>
      </View>
    </ScrollView>
  )
}

export default home